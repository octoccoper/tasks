function isStringPalindrome(str){


 if(typeof str === "string") {
	console.log("success");
	return str;
	} else {
	console.log("Entered parameter of isStringPalindrome function isn't string, it's ", typeof str);	
	return str;
    }
} 

